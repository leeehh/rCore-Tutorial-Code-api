# rCore ch2 API 实验：用户程序、系统调用与异常处理

## 实验目标

ch1 的内核直接在裸机上执行 Rust 代码。本章进一步让应用在用户态运行：应用通过系统调用请求内核服务，异常应用被终止后，内核继续运行下一个应用。

本实验围绕 `syscall()` 和 `trap_handler()` 展开，考察以下内容：

- 理解用户态 U 模式与内核态 S 模式，以及 `ecall` 如何进入内核。
- 根据寄存器约定分发系统调用，并把返回值交还给应用。
- 通过保存的上下文恢复应用，正确处理 `sepc`。
- 区分可返回的系统调用、结束应用的异常和本章不支持的 Trap。

`rust_main()`、汇编入口、应用加载器和具体系统调用均完整提供。学生不需要重写启动流程、保存寄存器的汇编或用户程序。

## 修改范围

只在以下两个文件中完成标记的接口，共 **2 处 TODO**：

| 文件 | 接口 | 数量 |
| --- | --- | --- |
| `os/src/syscall/mod.rs` | `syscall()` | 1 |
| `os/src/trap/mod.rs` | `trap_handler()` | 1 |

保留函数签名、`#[no_mangle]`、模块声明、常量和已有导入。如需私有辅助函数，只能在这两个文件内添加，且不能改变固定接口。不要新增系统调用或改动配套实现。

保持原日志宏、日志级别及 `LOG` 过滤方式。无需修改 `rust_main()`、`logging.rs` 或用户程序来展示结果。

## 实验要求

1. **代码修改范围**：仅允许在 [os/src/syscall/mod.rs](os/src/syscall/mod.rs) 和 [os/src/trap/mod.rs](os/src/trap/mod.rs) 中完成 `syscall()`、`trap_handler()` 两处 TODO，可在这两个文件内添加私有辅助函数。保留函数签名、`#[no_mangle]`、模块声明、常量、已有导入和原日志方式，不改动具体系统调用、应用加载器等配套实现。不得修改其他任何代码文件、构建配置或测试文件；可以新增实验报告等说明文档。

2. **静态分析与动态跟踪**：阅读 `ch2` 的参考实现，使用 GDB 跟踪用户系统调用和应用异常的处理过程，分析系统调用分发、上下文更新以及应用继续运行或终止的功能。操作可参考 [ch2 源代码分析与动态跟踪文档](../../blob/ch2/rcore-ch2-analyze.md)。报告应结合源代码说明调用顺序及其原因，记录关键断点、使用的 GDB 命令、观察到的状态和分析结论；可附必要的代码片段或源码链接。

3. **独立实现与对比**：依据本文接口契约完成两个函数，并按“验收”小节执行验收。在报告中对比自己的实现与参考实现的相同点和不同点，说明系统调用参数、`sepc`、返回值和异常控制流的处理，以及采用相关实现方式的原因；不能只列出代码文本的差异。

4. **主要问题与解决思路**：总结实验中遇到的主要问题，描述问题现象、原因分析、排查过程、解决思路及处理结果，结合代码、GDB 跟踪记录或运行输出说明依据。尚未解决的问题也应如实记录。

实验报告采用 Markdown（`.md`）格式，统一保存到仓库根目录下的 `reports/lab2.md`，至少包含静态分析与动态跟踪、独立实现对比、主要问题与解决思路三部分，并随实验代码提交。GDB 动态跟踪用于参考实现分析，完成后的功能验收仍使用本文规定的运行命令。

## 已提供的运行流程

### 从内核启动到用户程序

1. 汇编入口设置启动栈，进入 `rust_main()`。
2. `rust_main()` 清零 BSS、初始化日志，设置 Trap 入口并初始化批处理系统。
3. `batch::run_next_app()` 将当前应用复制到 `0x80400000`，建立初始 `TrapContext`。
4. `__restore` 恢复寄存器，通过 `sret` 进入 U 模式执行应用。
5. 应用执行 `ecall` 或发生异常时，进入 `__alltraps`，再调用待实现的 `trap_handler()`。

应用的二进制由构建脚本嵌入内核。批处理系统按顺序复用同一加载区域，不涉及文件系统、进程调度或页表地址转换。

U 模式不能执行本章样例中的特权指令或访问相应的特权寄存器。需要注意，本章的顺序加载和异常处理并不等同于实现了完整的应用内存隔离。

### Trap 入口与返回

`trap.S` 已完成以下工作：

- 在 `__alltraps` 中通过 `sscratch` 交换用户栈与内核栈。
- 在内核栈上保存应用的通用寄存器、`sstatus` 和 `sepc`，组成 `TrapContext`。
- 将上下文地址作为参数传给 `trap_handler()`。
- 在处理函数返回后，由 `__restore` 使用返回的上下文地址恢复应用并执行 `sret`。

因此，`trap_handler()` 应修改传入的上下文，不能重新构造一个空上下文覆盖应用状态，也不需要自行切换栈或执行 `sret`。

### `TrapContext`

已提供的数据结构如下：

```rust
#[repr(C)]
pub struct TrapContext {
    pub x: [usize; 32],
    pub sstatus: Sstatus,
    pub sepc: usize,
}
```

| 字段或寄存器 | 本实验中的含义 |
| --- | --- |
| `x[10]`，即 `a0` | 系统调用第一个参数，也用于保存返回值 |
| `x[11]`，即 `a1` | 系统调用第二个参数 |
| `x[12]`，即 `a2` | 系统调用第三个参数 |
| `x[17]`，即 `a7` | 系统调用编号 |
| `sstatus` | 恢复应用时使用的状态信息 |
| `sepc` | 恢复应用时开始执行的指令地址 |

发生 `ecall` 时，保存的 `sepc` 指向这条 `ecall` 指令本身。

## 接口一：`syscall()`

### 描述

根据系统调用编号，将三个机器字参数转换为相应接口的参数，并调用已经提供的系统调用实现。

### 签名

```rust
pub fn syscall(syscall_id: usize, args: [usize; 3]) -> isize
```

### 输入与分发规则

| 编号 | 常量 | 调用目标 | 参数含义与转换 |
| --- | --- | --- | --- |
| `64` | `SYSCALL_WRITE` | `sys_write` | `args[0]` 为 FD；`args[1]` 转为 `*const u8`；`args[2]` 为字节数 |
| `93` | `SYSCALL_EXIT` | `sys_exit` | `args[0]` 转为 `i32`，作为退出码；其余参数忽略 |

使用已有常量和函数，不要在分发层重新实现输出或应用加载逻辑。

### 输出

- `write`：原样返回 `sys_write()` 的 `isize` 结果。
- `exit`：`sys_exit()` 的返回类型为 `!`，它会开始下一个应用，不返回当前调用者。
- 其他编号：沿用本章行为，通过 `panic!` 报告不支持的系统调用编号；不能伪造成功或改为返回 `-1`。

### 约束

`sys_write()` 已提供，其有效输入为标准输出 FD `1`、有效可读的缓冲区以及合法 UTF-8 字节。正常返回写出的字节数。非法 FD 或无效 UTF-8 的既有处理不需要在本实验中修改。

分发函数只负责接口转换，不新增用户指针校验、页表转换、FD 管理或其他系统调用。不要用直接输出替代对 `sys_write()` 的调用。

`sys_exit()` 调用 `run_next_app()`。不得在此直接关闭 QEMU，或将 `exit` 改成一个普通返回函数，否则会跳过后续应用。

## 接口二：`trap_handler()`

### 描述

读取当前 Trap 原因和附加信息，处理用户系统调用或终止发生指定异常的应用。能够继续运行的应用，使用传入的上下文返回。

### 签名

```rust
#[no_mangle]
pub fn trap_handler(cx: &mut TrapContext) -> &mut TrapContext
```

### 输入

- `cx`：汇编入口保存在内核栈上的应用上下文。
- `scause` CSR：当前 Trap 原因，使用已有的 `scause::read()` 和 `cause()` 读取。
- `stval` CSR：当前 Trap 的附加信息，使用 `stval::read()` 读取，供不支持的 Trap 诊断使用。

### 用户系统调用

当原因为 `Trap::Exception(Exception::UserEnvCall)` 时：

1. 将 `cx.sepc` 恰好增加 `4`，使恢复地址越过这条 `ecall`。
2. 使用保存的 `a7` 作为编号，保存的 `a0`、`a1`、`a2` 作为三个参数，调用 `syscall()`。
3. 若调用返回，将其 `isize` 结果转换为 `usize`，写入 `cx.x[10]`。
4. 返回传入的同一个 `cx`，由汇编恢复应用。

必须在写回返回值之前取得调用参数。除 `sepc` 和返回值寄存器 `a0` 外，不得改变此路径下的其他上下文内容，包括 `sstatus`。

这里的 `4` 对应 `ecall` 的指令长度，不能因为目标支持压缩指令就改成 `2`。只有这个需要继续执行的系统调用分支更新 `sepc`；不能对所有异常统一跳过指令。

若调用的是 `sys_exit()`，控制流不会返回当前应用，因此不会进行该应用的返回值写回或上下文恢复。

### 终止异常应用

| Trap 原因 | 处理方式 |
| --- | --- |
| `Exception::StoreFault` 或 `Exception::StorePageFault` | 输出下面的 PageFault 提示，然后调用 `run_next_app()` |
| `Exception::IllegalInstruction` | 输出下面的 IllegalInstruction 提示，然后调用 `run_next_app()` |

沿用现有 `println!` 提示：

```text
[kernel] PageFault in application, kernel killed it.
[kernel] IllegalInstruction in application, kernel killed it.
```

`run_next_app() -> !` 会加载后续应用并恢复其新上下文，不返回旧应用。不能把这些异常当作内核错误直接 `panic!`，也不能返回原上下文再次执行出错指令。

### 其他 Trap

其余原因均按本章现有行为 `panic!`，诊断信息包含 Trap 原因和 `stval`。本实验不扩展加载异常、取指异常或中断的处理，也不增加定时器、抢占或异常恢复机制。

### 输出与返回约束

只有能返回的系统调用路径返回 `cx`。终止应用和不支持的 Trap 分支均不返回。保留 `#[no_mangle]` 及函数签名，使汇编可以继续按已有符号和调用方式进入处理函数。

## 配套接口与边界

| 已提供代码 | 作用 |
| --- | --- |
| `os/src/main.rs` | 内核初始化及进入批处理系统 |
| `trap::init()` | 设置 `stvec`，使 Trap 进入 `__alltraps` |
| `trap/context.rs` | 上下文结构、用户栈设置及初始用户态上下文构造 |
| `trap/trap.S` | 栈切换、寄存器保存恢复和 `sret` |
| `syscall/fs.rs::sys_write()` | 输出用户缓冲区内容 |
| `syscall/process.rs::sys_exit()` | 按现有 `trace!` 输出退出码并运行下一个应用 |
| `batch::run_next_app()` | 顺序加载应用，全部完成后正常退出 QEMU |
| `os/build.rs` | 生成应用映像的静态链接信息 |

应用之间不存在本章要求实现的调度器。退出和指定异常都交给批处理系统，不能只让处理函数返回来“切换”应用，也不能在某个应用结束时提前退出整个系统。

## 验收

在 `os` 目录下只运行：

```sh
make run BASE=2
```

Makefile 从 `ch2-api` 分支名提取章节号 `2`，自动构建并加载当前章节的 7 个应用。应用依次运行，无需输入命令，没有 shell 或 usertest 阶段，也不需要新增验证程序。

检查已有应用的实际输出与控制流：

| 应用 | 预期表现 |
| --- | --- |
| `ch2b_bad_address` | 非法写访问被终止，出现 PageFault 提示，随后加载下一个应用 |
| `ch2b_bad_instructions` | 用户态执行特权指令被终止，出现 IllegalInstruction 提示，随后继续 |
| `ch2b_bad_register` | 用户态访问特权寄存器被终止，出现 IllegalInstruction 提示，随后继续 |
| `ch2b_hello_world` | 输出 `Hello, world from user mode program!`，结束后继续 |
| `ch2b_power_3` | 输出计算结果及 `Test power_3 OK!` |
| `ch2b_power_5` | 输出计算结果及 `Test power_5 OK!` |
| `ch2b_power_7` | 输出计算结果及 `Test power_7 OK!` |

应能看到应用数量为 `7`，依次加载 `app_0` 至 `app_6`。异常样例不应执行到其失败提示；正常应用应完成自己的输出。最后出现 `All applications completed!`，QEMU 正常退出，命令成功结束。

批处理系统没有根据每个应用的退出码判定测试是否通过，因此不能只凭最后一句完成提示认定所有应用均正确运行，必须同时检查上述行为。

现有应用集合没有覆盖未知系统调用、不支持的 Trap，以及 `write` 返回值的全部边界。这些仍须遵守接口契约；本实验不因此增加验证命令或测例。

默认未设置 `LOG` 时日志过滤级别为 `Off`。`sys_exit()` 中的退出码日志使用 `trace!`，默认不显示；验收不要求为每个应用打印退出码，也不要修改日志方式来满足这一要求。

未完成的实验骨架可以编译和启动，但遇到第一个异常应用时会触发 `trap_handler()` 的 TODO，尚不能运行完整批次。补全两个接口后，再使用同一条命令验收。
